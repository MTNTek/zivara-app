export enum JobStatus {
  Draft = 'draft',
  Active = 'active',
  Closed = 'closed',
  Expired = 'expired',
}

export enum EmploymentType {
  FullTime = 'full_time',
  PartTime = 'part_time',
  ShiftBased = 'shift_based',
  Contract = 'contract',
}
